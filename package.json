{
  "name": "neon-overload",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "prisma generate && prisma db push",
    "dev": "NODE_ENV=development next dev",
    "build": "next build",
    "prestart": "prisma generate && prisma db push",
    "start": "NODE_ENV=production next start",
    "predeploy": "prisma generate && prisma db push",
    "deploy": "NODE_ENV=production next build && NODE_ENV=production next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@prisma/client": "4.16.2",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "babel-plugin-react-compiler": "1.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "prisma": "4.16.2",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
